package game.component.entity;

/**
 * A saleable interface, a class implement this interface means that item is saleable, like weapon, armor, etc.
 */
public interface Saleable {
    int getCost();
}
